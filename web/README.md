# web/

Browser-facing observer UI assets belong here.

This folder is scaffold-only right now.
No actual UI implementation has been started.

## Intended layout
- `css/` — stylesheets for the observer UI
- `js/` — browser scripts for polling, rendering, controls, and inspection
- `assets/` — static non-code assets such as icons, map art, or faction graphics
