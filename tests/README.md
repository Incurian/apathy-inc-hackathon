# tests/

Tests and validation harnesses belong here.

This folder is scaffold-only right now.

## Intended layout
- `sim/` — deterministic simulation tests
- `api/` — endpoint and serialization tests
- `integration/` — end-to-end tests across match lifecycle, replay, and controller integration
