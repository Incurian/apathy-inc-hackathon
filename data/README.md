# data/

Scenario data and generated replay artifacts belong here.

This folder is scaffold-only right now.

## Intended layout
- `scenarios/` — fixed scenario definitions, map coordinates, faction loadouts
- `replays/` — generated replay artifacts or cached latest-match output

Generated replay files should generally not be committed unless the team intentionally wants a known-good demo artifact under version control.
